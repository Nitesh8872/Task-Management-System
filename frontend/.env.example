# Backend URL — server root or API prefix (no trailing slash).
# Both forms work; /api is added automatically when omitted:
#   http://localhost:5000
#   https://your-app.up.railway.app/api
VITE_API_URL=http://localhost:5000
